# arr_sorted = sorted(list(map(int,input().split())))
# sorted function can be used when we need to sort the input while the input operation is going and the sort function is used when we have taken the input and then we need to sort the arr
# arr_sort = [5,4,6,7,3,2,1]
# arr_sort.sort()
# print(arr_sort)